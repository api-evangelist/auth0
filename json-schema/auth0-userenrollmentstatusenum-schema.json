{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "#/components/schemas/UserEnrollmentStatusEnum",
  "title": "UserEnrollmentStatusEnum",
  "type": "string",
  "description": "Status of this enrollment. Can be `pending` or `confirmed`.",
  "enum": [
    "pending",
    "confirmed"
  ]
}