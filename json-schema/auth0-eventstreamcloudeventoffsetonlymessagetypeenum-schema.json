{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "#/components/schemas/EventStreamCloudEventOffsetOnlyMessageTypeEnum",
  "title": "EventStreamCloudEventOffsetOnlyMessageTypeEnum",
  "type": "string",
  "description": "Identifies this as an offset-only heartbeat message (injected from the SSE event field).",
  "enum": [
    "offset-only"
  ]
}