{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "#/components/schemas/ConnectionAdminAccessTokenExpiresInGoogleApps",
  "title": "ConnectionAdminAccessTokenExpiresInGoogleApps",
  "description": "Expiration timestamp for the `admin_access_token` in ISO 8601 format. Auth0 uses this value to determine when to refresh the token.",
  "type": "string",
  "format": "date-time"
}