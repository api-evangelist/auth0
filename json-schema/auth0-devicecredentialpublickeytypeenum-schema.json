{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "#/components/schemas/DeviceCredentialPublicKeyTypeEnum",
  "title": "DeviceCredentialPublicKeyTypeEnum",
  "type": "string",
  "description": "Type of credential. Must be `public_key`.",
  "enum": [
    "public_key"
  ]
}