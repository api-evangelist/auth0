{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "#/components/schemas/EventStreamCloudEventGroupCreatedTypeEnum",
  "title": "EventStreamCloudEventGroupCreatedTypeEnum",
  "type": "string",
  "description": "The event type (injected from the SSE event field).",
  "enum": [
    "group.created"
  ]
}