{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "#/components/schemas/CreateConnectionRequestContentExact",
  "title": "CreateConnectionRequestContentExact",
  "description": "Create a connection with strategy=exact",
  "allOf": [
    {
      "type": "object",
      "properties": {
        "strategy": {
          "const": "exact"
        },
        "options": {
          "$ref": "#/components/schemas/ConnectionOptionsExact"
        }
      },
      "required": [
        "strategy"
      ]
    },
    {
      "$ref": "#/components/schemas/CreateConnectionCommon"
    }
  ]
}